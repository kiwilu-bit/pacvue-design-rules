---
description: Pacvue 表格（Table）AI 代码生成速查——区域结构、行高、固定列、多选联动、Toolbar、EP override。完整规范见 04-areas/。
globs: ["**/*.vue", "**/*.html", "**/*.css"]
alwaysApply: false
---

# 10 · 表格规范（Table）速查

> 完整规范：
> - Toolbar → `04-areas/toolbar-area.md`
> - 水平滚动条 → `04-areas/scrollbar-area.md`
> - 表格本体 → `04-areas/table-area.md`
>
> 数据来源：Figma `wgvYWGdj9Bfq2HSEpRbnLN`，节点 `19120-18385`、`11255-130924`

---

## 完整区域结构（从上到下，各区零间距）

```
筛选栏 Filter Bar   → 自适应高度，padding: 16px 24px
操作栏 Toolbar      → 68px，border-bottom: 1px solid #EDEEF1
水平滚动条          → 16px，仅列超宽时出现
表头 Header         → 48px，sticky
数据行 Data Row     → 52px（最小），hover/selected 品牌色背景
汇总行 Total        → 52px，font-weight: 600
分页器 Pagination   → 64px，border-top: 1px solid #EDEEF1
```

---

## Toolbar 速查

```css
.toolbar {
  height: 68px;
  padding: 0 24px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-bottom: 1px solid #edeef1;
}
```

| 按钮类型 | 背景 | 边框 | 文字 |
|---|---|---|---|
| Primary（Create） | `var(--pcv-brand)` | 无 | `#fff` |
| Secondary | 透明 | `1px solid var(--pcv-brand)` | `var(--pcv-brand)` |
| Disabled（Bulk Op）| `rgba(130,133,139,.05)` | `1px solid #DEDFE3` | `#B2B2B8` |
| Active（Bulk Op） | 透明 | `1px solid var(--pcv-brand)` | `var(--pcv-brand)` |

右侧图标：`24×24px`，`#66666C`，hover → `var(--pcv-brand)`；分隔线 `2px×24px #D2D4D9`

---

## 表格尺寸速查

| 元素 | 高度 | 说明 |
|---|---|---|
| Header | 48px | sticky，白底 |
| Data Row | 52px | hover `rgba(brand,0.04)`，selected `rgba(brand,0.06)` |
| Total Row | 52px | `font-weight: 600` |

列宽参考：Name ≥ 280px，状态列 88~120px，数值列 106~149px（右对齐），操作列 55px

---

## 固定列关键规则

⚠️ **固定列背景必须用 `color-mix`，不能用 `rgba`（会透视后面列文字）**

```css
/* ✅ 正确 */
tr:hover td.sticky {
  background: color-mix(in srgb, var(--pcv-brand) 4%, #ffffff);
}
tr.selected td.sticky {
  background: color-mix(in srgb, var(--pcv-brand) 6%, #ffffff);
}

table { border-collapse: separate; border-spacing: 0; }
```

阴影：左固定列 `box-shadow: 10px 0 14px 0 rgba(0,0,0,0.06)`；右固定列 `-10px 0 14px 0 rgba(0,0,0,0.06)`

---

## 多选联动速查

- 空框 → 全选当前页（弹出菜单：Select All Page / Select Current Page / Deselect All）
- 有选中行 → Toolbar Bulk Op 激活为品牌色边框
- Checkbox 已选：`background: var(--pcv-brand)`，圆角 3px

---

## Element Plus 关键 Override

```css
/* 表头白底 48px */
.el-table th.el-table__cell {
  background: #ffffff; height: 48px; padding: 0;
  color: #45464f; font-size: 14px; font-weight: 500;
  border-bottom: 1px solid #edeef1;
}
.el-table th.el-table__cell .cell { padding: 0 16px; line-height: 48px; }

/* 数据行 52px */
.el-table td.el-table__cell {
  height: 52px; padding: 0;
  border-bottom: 1px solid #edeef1; color: #66666c; font-size: 14px;
}
.el-table td.el-table__cell .cell { padding: 0 16px; line-height: 20px; }

/* hover / selected */
.el-table--enable-row-hover .el-table__body tr:hover > td {
  background: rgba(var(--pcv-brand-rgb), 0.04);
}
.el-table__body tr.el-table__row--selected > td {
  background: rgba(var(--pcv-brand-rgb), 0.06);
}

/* 固定列必须实色 */
.el-table__body tr:hover > td.el-table-fixed-column--left,
.el-table__body tr:hover > td.el-table-fixed-column--right {
  background: color-mix(in srgb, var(--pcv-brand) 4%, #ffffff);
}
.el-table__body tr.el-table__row--selected > td.el-table-fixed-column--left,
.el-table__body tr.el-table__row--selected > td.el-table-fixed-column--right {
  background: color-mix(in srgb, var(--pcv-brand) 6%, #ffffff);
}

/* 排序激活 */
.el-table .ascending  .sort-caret.ascending  { border-bottom-color: var(--pcv-brand); }
.el-table .descending .sort-caret.descending { border-top-color:    var(--pcv-brand); }

/* 分隔线 */
.el-table::before { background: #edeef1; }

/* 横向滚动条 */
.el-table__body-wrapper::-webkit-scrollbar       { height: 8px; }
.el-table__body-wrapper::-webkit-scrollbar-thumb  { background: #d2d4d9; border-radius: 7px; }
.el-table__body-wrapper::-webkit-scrollbar-track  { background: #f5f5f5; border-radius: 7px; }
```

---

## 状态点颜色

| 状态 | 颜色 |
|---|---|
| Active | `#28C76F` |
| Paused | `#82858B` |
| Error | `#EA5455` |
| Warning | `#FF9F43` |
