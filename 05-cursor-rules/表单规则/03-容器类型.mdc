---
description: Pacvue 表单规则 03 — 容器类型（弹窗、筛选栏、浮窗、表单页）
alwaysApply: true
---

# 03 · 表单容器类型

Pacvue 表单内容出现在以下 4 种容器中，每种容器有固定的尺寸规范，不可随意自定义。

---

## 3.1 弹窗（Dialog / Modal）

**适用场景：** 新建、编辑、二次确认等操作，通常是从列表页触发的浮层。

```
┌─────────────────── 720px ────────────────────┐
│ [Header 56px] 弹窗标题 + 关闭按钮            │
├───────────────────────────────────────────────┤
│                                               │
│  [Body 内容区]                                │
│   padding: 24px 40px                          │
│   字段内容，自适应高度                         │
│                                               │
├───────────────────────────────────────────────┤
│ [Footer 68px]          [Cancel] [OK/Create]  │
└───────────────────────────────────────────────┘
```

**关键数值：**

| 区域 | 数值 |
|------|------|
| 弹窗宽度 | **720px** |
| Header 高度 | **56px** |
| Footer 高度 | **68px** |
| 内容区 padding | **24px 40px** |
| Footer 按钮 gap | **20px** |
| Footer 右 padding | **24px** |

**Footer 按钮规格（弹窗内）：**
- 高度：36px，`border-radius: 4px`
- 宽度：固定 88px（`min-width: 88px`）
- 主要按钮：`bg: var(--pcv-brand)`，白色文字
- 灰色/取消按钮：`border: 1px solid #DEDFE3`，`color: #82858b`

---

## 3.2 筛选组件（Filter Bar）

**适用场景：** 数据列表页顶部的筛选区域，水平排列多个筛选控件。

**构成：** 按钮、输入框、选择器等横向组合。

**布局规则：**
- 水平排列，`gap: 8px`（筛选控件间）
- 控件高度统一：`36px`（small 尺寸）
- 搜索输入框：左侧有搜索图标（20×20px），padding-left 预留图标空间
- 右侧操作按钮（Search、Reset）与筛选项分组，独立靠右或靠左对齐

**典型结构：**
```
[过滤按钮] [场景按钮] │ [Select A ▾] [Select B ▾] [Select C ▾] │ [🔍 Search ASIN]    [Search] [×]
```

---

## 3.3 浮窗 / 下拉弹窗（Popover / Dropdown）

**适用场景：** 在浮窗或下拉弹窗中进行选择类操作。

**三种子类型：**

| 子类型 | 场景 |
|-------|------|
| **日期选择器（Date Picker）** | 日期范围选择，弹出日历 |
| **下拉选择（Select Dropdown）** | 单选/多选列表 |
| **级联选择（Cascade）** | 多级联动选择（两个 256px 面板并排） |

**下拉面板通用规则：**
- `background: #fff`
- `border: 1px solid #DEDFE3`
- `border-radius: 4px`
- `box-shadow: 0 8px 24px rgba(0,0,0,0.12)`
- 选项高度：36px（与 input 等高）
- 选项 hover：`background: var(--pcv-hover-bg)`
- 选中项：`color: var(--pcv-brand)`，`background: var(--pcv-selected-bg)`

---

## 3.4 表单页面（Form Page）

**适用场景：** 创建/编辑类全页面。

按 M / L / XL 档位规范执行，详见：
→ `.cursor/rules/pacvue-create-page-template.mdc`

**快速对照：**

| 档位 | 宽度 | 适用 |
|------|------|------|
| M | 720px | 简单表单，字段 ≤ 8 |
| L | 960px | 复杂分组，多步骤 |
| XL | 1330px | 含预览/多列/右侧面板 |

---

## 3.5 容器选择判断

```
弹出/浮层操作？
  ├─ 是 → 弹窗（720px）或浮窗（下拉/日期）
  └─ 否 → 表单页面（M/L/XL 档位）

列表页上方的筛选区域？
  └─ 筛选组件（Filter Bar）
```
