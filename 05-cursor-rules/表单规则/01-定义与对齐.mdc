---
description: Pacvue 表单规则 01 — 表单定义与对齐方式
alwaysApply: true
---

# 01 · 表单定义与对齐方式

## 1.1 表单定义

表单是用于信息添加、录入的页面类型，由以下控件组成：
- 输入框（Input）
- 选择器（Select / Dropdown）
- 单选框（Radio）
- 多选框（Checkbox）
- 开关（Toggle/Switch）
- 其他信息输入控件

**行为规则：**
- 页面加载后，默认自动聚焦第一个可交互控件
- 支持 Tab 键顺序切换填充表单元素（键盘可访问性）

> 组件详细样式规范（输入框、选择器、单选框、多选框）待补充。

---

## 1.2 对齐方式

### Label 与输入框
- Label 在上，输入框在下，**垂直对齐**
- Label 与输入框之间：`gap: 4px`

```
[Label 文字]          ← 14px / 500 / #45464f
[──────────────────]  ← 输入框 / 选择器，height: 36px
```

### 同行多个组件（水平排列）
- 同一行内的多个组件**水平对齐**
- 保持统一的列宽和列间距（`gap: 20px`）

### 带开关（Toggle）的字段
- Label 与 Toggle 左右布局（`display: flex; flex-direction: row`）
- 与其他组件保持统一的垂直对齐

```
[* State]  [○ Toggle]   ← 同行，gap 12px
```

### 多列布局
- 两列字段：`grid-template-columns: 1fr 1fr; gap: 20px`（横向列间距 20px）
- 保持每列内部组件左对齐

---

## 1.3 对齐示例结构

```
┌─────────────────────────────────────────────┐
│ * Field Label A          Field Label B       │
│ [─────────────────────] [──────────────────] │  ← 2列，gap: 20px
│                                              │
│ * Field Label C                              │
│ [───────────────────────────────────────────] │  ← 1列，全宽
│                                              │
│ * State        [○ Toggle]                   │  ← label + toggle 同行
└─────────────────────────────────────────────┘
```
