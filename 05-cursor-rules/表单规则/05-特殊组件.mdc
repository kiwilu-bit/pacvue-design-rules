---
description: Pacvue 表单规则 05 — 特殊组件（Name 列表、输入框+按钮组合、侧边栏复选框）
alwaysApply: true
---

# 05 · 特殊表单组件

## 5.1 Name 类列表输入（可动态增删行）

**适用场景：** 当字段允许用户添加多条「name」类条目时（如：Add Catalog Name、Add Tag 等）。

**高度行为规则：**

| 条目数量 | 高度行为 |
|---------|---------|
| ≤ 4 条 | 容器高度随条目数量**自动伸展** |
| ≥ 5 条 | 容器高度**固定**，出现**滚动条**查看更多 |

**结构：**
```
┌─ 输入框区域（可滚动）─────────────────────┐
│ [输入项 1]                               │
│ [输入项 2]                               │
│ [输入项 3]                               │
│ [输入项 4]                               │
│ [输入项 5] ← 第 5 条开始固定高度+滚动     │
└───────────────────────────────────────────┘
[+ Add Name]
```

---

## 5.2 输入框 + 操作按钮组合

**适用场景：** 输入框右侧需要附带一个操作按钮（如「Manage」「Browse」「Validate」）。

**布局规则：**
```
[─── Input (flex: 1, 宽度弹性) ─────────────] [按钮 72px]
                                    ↑ gap: 20px
```

**CSS：**
```css
.input-with-action {
  display: flex;
  align-items: center;
  gap: 20px;
}
.input-with-action .field-input { flex: 1; }
.input-with-action .btn-action {
  width: 72px;
  flex-shrink: 0;
  height: 36px;
}
```

**按钮样式：** 小号次级按钮（Secondary Small）
- `height: 36px`
- `border: 1px solid #DEDFE3`
- `color: #66666c`
- `border-radius: 4px`

---

## 5.3 带标题和复选框的侧边栏（Column Picker）

**适用场景：** 列管理、字段选择等场景，左侧固定宽度侧边栏。

**结构：**
```
┌── 280px ──────────────────────────────┐
│ [分组标题]                         48px│  ← bg: 透明, padding: 15px 8px
├───────────────────────────────────────┤
│ [□] column_title                   52px│  ← 无缩进
├───────────────────────────────────────┤
│  ▶ [□] column_title                52px│  ← 有缩进（子项，左侧 32px）
├───────────────────────────────────────┤
│  ▶ [□] column_title                52px│
└───────────────────────────────────────┘
```

**关键尺寸：**
- 侧边栏宽：`280px`
- 分组标题行高：`48px`，`font-size: 12px`（或 13px），`color: #45464f`
- 普通选项行高：`52px`，`padding: 16px 8px`
- 复选框：`20×20px`，与文字 `gap: 8px`
- 子项缩进：左侧 `padding-left: 40px`（箭头图标 24px + gap 8px + 8px padding）
- 箭头图标：`24×24px`，折叠/展开状态切换

---

## 5.4 带 OR 分隔的条件组

**适用场景：** 规则创建中，多个条件块之间用 OR 分隔。

**间距规则：**
```
[条件组 A]
    ↕ 12px
   OR
    ↕ 12px
[条件组 B]
```

- OR 文字：`14px / 400 / color: #45464f`
- OR 上下各留：`12px`（内边距，从 Figma 标注 `spacing Frame 318 - OR: 12px` 确认）

---

## 5.5 带「Add Row」的复杂条件组

**适用场景：** 每个条件组内可动态添加多行（如 Keyword 条件）。

**行结构：**
```
┌── 条件组容器（内边距 20px）───────────────────────────┐
│ [Select A (246px)]  [Select B (310px)]  [× 关闭 20px]│ ← 行1
│                              ↕ 12px                   │
│ [Select A (246px)]  [Select B (310px)]  [× 关闭 20px]│ ← 行2
│                              ↕ 12px                   │
│ [+ Add]                                               │ ← 新增按钮
└───────────────────────────────────────────────────────┘
```

- 两列选择器横向 gap：`258 - 246 = 12px`（即 Select A 右侧到 Select B 左侧 12px）
- 关闭图标：`20×20px`，与右侧 Select B 距离 `12px`
- 新增按钮：文字按钮加图标，`color: var(--pcv-brand)`

> 组件详细规范（输入框、选择器、单选框、多选框）待后续补充。
