---
description: Pacvue 分页器（Pagination）AI 代码生成速查——布局、页码按钮、每页下拉、Go to page、显示逻辑、EP override。完整规范见 04-areas/pagination-area.md。
globs: ["**/*.vue", "**/*.html", "**/*.css"]
alwaysApply: false
---

# 12 · 分页器规范（Pagination）速查

> 完整规范（含显示逻辑、EP 对比表）：`04-areas/pagination-area.md`  
> 数据来源：Pacvue Design System Figma `wgvYWGdj9Bfq2HSEpRbnLN`，节点 `7347:114707`

---

## 布局结构

```
左侧：Total N entries,  [10/page ↕]
右侧：[<] [1][2][3][4][5][···][N] [>]   Go to page [__]
      ←────────── justify-content: space-between ──────────→
      height: 64px  ·  border-top: 1px solid #EDEEF1
```

---

## 显示逻辑

- 切换每页数量后，当前页数据 ≤ 每页数量 → **分页器消失**，仅保留页码切换器
- 到达最后一页，数据量 ≤ 每页数量 → **分页器保留**

---

## 关键尺寸速查

| 元素 | 值 | 注意 |
|---|---|---|
| 容器高度 | 64px | `flex-shrink: 0` |
| 页码按钮 | 32×32px，圆角 4px | 当前页仅改边框+文字色，**无背景填充** |
| 当前页 | `border: 1px solid var(--pcv-brand)`，`color: var(--pcv-brand)`，`font-weight: 500` | — |
| 省略号 | 无 border，`color: #82858B`，不响应 hover | — |
| 每页下拉高度 | 32px，圆角 **5px** | 非标准 4px！ |
| 每页下拉边框 | `1px solid #D8D6DE` | 比条件框 `#DEDFE3` 稍深 |
| Go to page 输入框 | 58×32px，圆角 4px，文字居中 | — |

---

## 关键 CSS

```css
.pagination {
  height: 64px;
  padding: 0 24px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-top: 1px solid #edeef1;
  background: #ffffff;
  flex-shrink: 0;
}

.page-btn {
  width: 32px; height: 32px;
  border-radius: 4px;
  border: 1px solid #dedfe3;
  background: transparent;
  font-size: 14px; color: #66666c;
  display: grid; place-items: center;
  cursor: pointer; flex-shrink: 0;
}
.page-btn:hover          { border-color: var(--pcv-brand); color: var(--pcv-brand); }
.page-btn.active         { border-color: var(--pcv-brand); color: var(--pcv-brand); font-weight: 500; }
.page-btn.ellipsis       { border-color: transparent; cursor: default; color: #82858b; }
.page-btn.ellipsis:hover { border-color: transparent; color: #82858b; }

.page-size-select {
  height: 32px;
  border: 1px solid #d8d6de;
  border-radius: 5px;   /* 注意：5px 非 4px */
  font-size: 14px; font-weight: 500; color: #5e5873;
}

.page-goto-input {
  width: 58px; height: 32px;
  border: 1px solid #dedfe3; border-radius: 4px;
  text-align: center; font-weight: 500; color: #45464f;
}
.page-goto-input:focus { border-color: var(--pcv-brand); outline: none; }
```
