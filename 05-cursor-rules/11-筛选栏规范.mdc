---
description: Pacvue 筛选栏（Filter Bar）AI 代码生成速查——布局规则、Search/Reset 位置判断、条件框样式、响应式列数。完整规范见 04-areas/filter-bar-area.md。
globs: ["**/*.vue", "**/*.html", "**/*.css"]
alwaysApply: false
---

# 11 · 筛选栏规范（Filter Bar）速查

> 完整规范（含图示、四种典型布局、HTML 模板）：`04-areas/filter-bar-area.md`  
> 数据来源：Pacvue Design System Figma `wgvYWGdj9Bfq2HSEpRbnLN`

---

## 核心规则

1. 默认 **4 列等宽 grid**，`gap: 16px`，filter 最小宽度 **263px**
2. **Search / Reset 位置判断**：
   - 最后一行有空位 → **同行靠右**（`justify-content: space-between`）
   - 最后一行已满 → **独占新行**（`justify-content: flex-end`）
3. 条件框高度 **36px**，圆角 **4px**；Search/Reset 圆角 **6px**（不同！）
4. 响应式：`repeat(auto-fill, minmax(263px, 1fr))`，超宽自动扩 5/6 列，无需 media query
5. 筛选栏与 Toolbar 之间**无间距**

---

## 关键 CSS

```css
/* 外层容器 */
.filter-bar {
  padding: 16px 24px;
  border-bottom: 1px solid #edeef1;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

/* 条件行（响应式列数） */
.filter-row {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(263px, 1fr));
  gap: 16px;
}

/* 最后行有空位：同行两端 */
.filter-last-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 16px;
}

/* 最后行已满：Search/Reset 独占行靠右 */
.filter-actions {
  display: flex;
  justify-content: flex-end;
  gap: 16px;
}
```

---

## 条件框速查

| 元素 | 值 |
|---|---|
| 高度 | 36px |
| 边框 | `1px solid #DEDFE3`，hover → `var(--pcv-brand)` |
| 圆角 | 4px |
| 内部结构 | `Label（Medium #45464F）｜ 1px 分隔线 ｜ Value ｜ ↓ 20px` |
| Search 按钮圆角 | **6px**（与条件框不同） |
| Reset 按钮 | `36×36px`，圆角 **6px** |
